#include <stdio.h>

/**
 * main - Print thr program name
 *
 * @argc: size of command line arguments
 *
 * @argv: An array of size argc
 *
 * Return: 0
 */

int main(int argc __attribute__((unused)), char *argv[])
{
	printf("%s\n", argv[agrc * 0]);

	return (0);
}
