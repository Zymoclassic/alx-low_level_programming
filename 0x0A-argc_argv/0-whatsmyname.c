#include <stdio.h>
#include "main.h"

/**
 * main - prints the program name
 * @argc: number of command line arguments
 * @argv: name of array
 * return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
